version: '3.8'

services:
  vocab-survival:
    build: .
    container_name: vocab-survival
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/backend/data
    environment:
      - PORT=8000
      - HOST=0.0.0.0
